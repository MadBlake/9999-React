// src/hooks/__tests__/useForm.test.ts
import { describe, it, expect } from 'vitest'

describe('useForm', () => {
  it('is deprecated - use useState and validation in TaskForm.tsx', () => {
    // Este hook ha sido eliminado porque los alumnos deben implementar
    // useState y validación directamente en TaskForm.tsx como parte del examen
    expect(true).toBe(true)
  })
})
